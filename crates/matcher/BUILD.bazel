load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "grep_matcher",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    deps = [
        # Add dependencies as needed based on imports in the code
    ],
)
