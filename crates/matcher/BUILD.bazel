load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "grep_matcher",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    deps = [
        "@crate_index//:bstr",
        "@crate_index//:memchr",
        "@crate_index//:regex",
        "@crate_index//:regex_syntax",
    ],
)
