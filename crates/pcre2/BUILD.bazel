load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")
load("@crate_index//:defs.bzl", "all_crate_deps")

rust_library(
    name = "pcre2",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "grep_pcre2",
    edition = "2018",
    visibility = ["//visibility:public"],
    deps = [
        "//crates/matcher:matcher",
    ] + all_crate_deps(
        normal = True,
    ),
)

rust_test(
    name = "lib_tests",
    crate = ":pcre2",
    edition = "2018",
    deps = all_crate_deps(
        normal_dev = True,
    ),
)
