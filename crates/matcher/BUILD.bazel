load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "grep_matcher",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "@crate_index//:memchr",
    ],
    visibility = ["//visibility:public"],
)

rust_test(
    name = "integration",
    srcs = ["tests/tests.rs"],
    deps = [
        ":grep_matcher",
        "@crate_index//:regex",
    ],
)
