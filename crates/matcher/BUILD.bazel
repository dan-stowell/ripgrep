# Temporary filegroup to allow building the target during incremental Bazel
# migration. This avoids loading external rules_rust at parse time.
filegroup(
    name = "grep_matcher",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
)
