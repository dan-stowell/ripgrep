load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "grep_matcher",
    srcs = glob(["src/*.rs"]),
    crate_root = "src/lib.rs",
    deps = [
        "@crate_index//:bstr",
        "@crate_index//:memchr",
    ],
)
