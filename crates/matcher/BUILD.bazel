load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "grep_matcher",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "@crate_index//:memchr",
        "@crate_index//:regex",
        "@crate_index//:bstr",
        "@crate_index//:lazy_static",
        "@crate_index//:log",
        "@crate_index//:cfg-if",
        # Add common dependencies that might be used
        "@crate_index//:aho_corasick",
        "@crate_index//:regex-automata",
    ],
    proc_macro_deps = [],
    visibility = ["//visibility:public"],
)

rust_test(
    name = "tests",
    srcs = glob(["tests/**/*.rs"]),
    deps = [
        ":grep_matcher",
        ":testutil",
        "@crate_index//:regex",
        "@crate_index//:bstr",
        "@crate_index//:aho_corasick",
        "@crate_index//:regex-automata",
    ],
)
rust_library(
    name = "testutil",
    srcs = ["tests/util.rs"],
    deps = [
        ":grep_matcher",
        "@crate_index//:regex",
        "@crate_index//:bstr",
        "@crate_index//:aho_corasick",
        "@crate_index//:regex-automata",
    ],
    visibility = ["//tests:__subpackages__"],
)
