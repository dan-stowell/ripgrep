load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "grep_matcher",
    srcs = [
        "src/lib.rs",
    ],
    edition = "2021",
    deps = [
        "@crate_index//:aho_corasick",
        "@crate_index//:bstr",
        "@crate_index//:memchr",
        "@crate_index//:regex-syntax",
    ],
)
